class Solution {
public:
    int minMoves(vector<int>& nums) {
        int mini = nums[0];
        int count = 0;

        for(int i = 0; i < nums.size(); i++)
        {
            mini = min(mini, nums[i]);
        }

        for(int i = 0; i < nums.size(); i++)
        {
            count += nums[i] - mini;
        }

        return count;
    }
};
