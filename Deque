#include<iostream>
#include<vector>
#include<list>
#include<deque>
#include<stack>
#include<queue>
#include<map>
#include<unordered_map>
#include<set>
#include<unordered_set>
#include<algorithm>
using namespace std;
int main()
{
   deque<int>l;
   l.push_back(1);
   l.push_back(2);
   l.push_front(4);
   l.push_front(5); 
   l.push_back(3);
   l.pop_back();
   l.pop_front();
   l.insert(l.begin(),0);
   l.erase(l.begin());
   cout<<"size:"<<l.size()<<endl;
   cout<<"Value of index 2:"<<l[2]<<endl;
   cout<<"fornt:"<<l.front()<<endl;
   cout<<"back:"<<l.back()<<endl;
   for(int val:l)
   {
    cout<<val<<" ";
   }
   cout<<endl;
   cout<<endl;
   deque<int>d1={1,2,3,4,5};
   for(int val:d1)
   {
    cout<<val<<" ";
   }
   cout<<endl;
   cout<<endl;
   deque<int>d2(3,5);
   for(int val:d2)
   {
    cout<<val<<" ";
   }
   cout<<endl;
   deque<int>d3(d2);
   for(int val:d3)
   {
    cout<<val<<" ";
   }
   cout<<endl;
    return 0;
}
