#include <iostream>
#include<climits>
using namespace std;

int main()
{
    int arr[3][4];
    cout<<"Enter Array Elements:"<<endl;

    for (int row = 0; row < 3; row++)
    {
        for (int col = 0; col < 4; col++)
        {
            cin>>arr[row][col];
        }
    }

    cout<<"Print Array Elements is:"<<endl;

    for (int row = 0; row < 3; row++)
    {
        for (int col = 0; col < 4; col++)
        {
            cout<<arr[row][col]<<" ";
        }
        cout<<endl;
    }

    int maxcolsum=INT_MIN;

    for (int col = 0; col < 4; col++)
    {
        int sum=0;
        for (int row = 0; row < 3; row++)
        {
            sum=sum+arr[row][col];
        }
        maxcolsum=max(maxcolsum,sum);
    }

    cout<<"The maximum column sum is:"<<maxcolsum<<endl;

    return 0;
}
