#include<iostream>
#include<vector>
#include<list>
#include<deque>
#include<stack>
#include<queue>
#include<map>
#include<unordered_map>
#include<set>
#include<unordered_set>
#include<algorithm>
using namespace std;
int main()
{
    vector<int>v;
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);
    v.emplace_back(4);
    v.emplace_back(5);
    v.pop_back();
    v.insert(v.begin()+4,5);
    v.erase(v.begin()+4);
    cout<<"Index 2 value is:"<<v[2]<<endl;
    cout<<"Index 3 value is:"<<v.at(3)<<endl;
    cout<<"front:"<<v.front()<<endl;
    cout<<"Back:"<<v.back()<<endl;
    cout<<"size:"<<v.size()<<endl;
    cout<<"capacity:"<<v.capacity()<<endl;
    for(int val:v)
    {
        cout<<val<<" ";
    }

    v.clear();
    cout<<endl;
    cout<<"Empty:"<<v.empty()<<endl;

    cout<<endl;
    vector<int>v1={1,2,3,4,5};
    for(int val:v1)
    {
        cout<<val<<" ";
    }
    cout<<endl;
    cout<<endl;
    vector<int>v2(10,-1);
    for(int val:v2)
    {
        cout<<val<<" ";
    }
    cout<<endl;
    cout<<endl;
    vector<int>v3(v2);
    for(int val:v3)
    {
        cout<<val<<" ";
    }
    cout<<endl;
    cout<<endl;

    for(auto it=v1.begin();it!=v1.end();it++)
    {
        cout<<*(it)<<" ";
    }
    cout<<endl;
    cout<<endl;
    for(auto it=v1.rbegin();it!=v1.rend();it++)
    {
        cout<<*(it)<<" ";
    }
    return 0;
}
