#include <iostream>
#include<climits>
using namespace std;


int main()
{
    int arr[5]={-1,3,-2,4,5};
    int currsum=0;
    int maxsum = INT_MIN;
    for(int i=0;i<5;i++)
    {
        currsum+=arr[i];
        maxsum=max(maxsum,currsum);
        if(currsum<0)
        {
            currsum=0;
        }
    }
    cout<<maxsum;
    return 0;
}
