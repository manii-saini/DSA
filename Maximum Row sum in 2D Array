#include <iostream>
#include<climits>
using namespace std;

int main()
{
    int arr[3][4];
    cout<<"Enter Array Elements:"<<endl;

    for (int row = 0; row < 3; row++)
    {
        for (int col = 0; col < 4; col++)
        {
            cin>>arr[row][col];
        }
    }

    cout << "simple matrix" << endl;

    for (int row = 0; row < 3; row++)
    {
        for (int col = 0; col < 4; col++)
        {
            cout << arr[row][col] << " ";
        }
        cout << endl;
    }

    int maxrowsum=INT_MIN;

    for (int row = 0; row < 3; row++)
    {
        int sum=0;
        for (int col = 0; col < 4; col++)
        {
            sum=sum+arr[row][col];
        }
        maxrowsum=max(maxrowsum,sum);
    }

    cout<<"The maxrowsum is:"<<maxrowsum;
    return 0;
}
